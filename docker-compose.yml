services:
  ragtox:
    build: .
    container_name: ragtox
    restart: unless-stopped
    environment:
      RAG_BIND: 0.0.0.0
      RAG_PORT: 8767
      RAG_READONLY: 1
      RAG_ALLOWED_HOSTS: ragtox.pedrojuezmartel.com
      RAG_ALLOWED_ORIGINS: https://ragtox.pedrojuezmartel.com
      # La clave nunca va en la imagen: se lee del .env del servidor
      LLM_PROVIDER: ${LLM_PROVIDER:-gemini}
      LLM_MODEL: ${LLM_MODEL:-gemini-3.8-flash}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
    ports:
      # Solo accesible desde el propio servidor; Caddy lo publica con HTTPS.
      - "127.0.0.1:8767:8767"
    mem_limit: 512m
